{
  "name": "dependify",
  "version": "0.0.1",
  "description": "Browserify plugin to produce a UMD standalone with AMD, CommonJS or global dependencies.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "browserify",
    "browserify-plugin",
    "amd",
    "umd"
  ],
  "author": "rturnq (rturnq@gmail.com)",
  "license": "MIT",
  "dependencies": {
    "string-format-js": "^0.1.5",
    "through2": "^2.0.0"
  }
}
